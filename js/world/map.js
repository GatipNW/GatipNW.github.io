// ============================================
// map.js — layout ห้องสตูดิโอ (top-down)
// หน่วยเป็น world px — กล้องจะ scale ตามจอเอง
// ============================================

export const MAP = {
  width: 1600,
  height: 1000,

  // ★ 24 → 56 (2026-07-20): ผนังซ้าย/ขวา/ล่างเดิมเป็นแค่แถบหินบางๆ ไม่มีหน้าผนัง
  //   เจ้าของติว่า "ผนังรอบห้องภาพไม่ชัด" → วาดให้มีความสูงจริง
  //   collision ต้องขยายตามภาพ ไม่งั้นเดินทะลุเข้าไปในผนัง (บั๊กแบบเดียวกับหน้าต่าง)
  wallThickness: 56,

  // ★ ผนังเหนือ "เห็นหน้าผนัง" สูง 110px (หน้าต่าง 3 บานฝังอยู่ในนี้)
  //   ต้องตรงกับ NORTH_WALL_H ใน renderer.js + WALL_H ใน Python/gen_room.py
  //   เดิม collision บนใช้แค่ 24px → ผู้เล่นเดินขึ้นไปทับหน้าต่างได้ (บั๊ก 2.5D)
  northWallH: 110,

  // จุดเกิดผู้เล่น / จุดกลางวงเวท — ★ 2026-07-20
  //   620 → 543 (กลางพื้นที่เดินได้) → 500 (กลางห้องจริงตามสายตาเจ้าของ)
  //   ★ ถ้าย้ายค่านี้ ต้องแก้ SPAWN ใน tools/gen_room_v2.py ให้ตรงด้วย (= y + 30)
  spawn: { x: 800, y: 500 },
};

// กำแพงรอบห้อง (AABB: x, y, w, h)
const T = MAP.wallThickness;
const NH = MAP.northWallH;
MAP.walls = [
  { x: 0, y: 0, w: MAP.width, h: NH },                   // บน — เต็มความสูงหน้าผนัง
  { x: 0, y: MAP.height - T, w: MAP.width, h: T },       // ล่าง
  { x: 0, y: 0, w: T, h: MAP.height },                   // ซ้าย
  { x: MAP.width - T, y: 0, w: T, h: MAP.height },       // ขวา
];

// ขอบเขตพื้นที่เดินได้จริง (ใช้โดยโปริ่ง/แฟรี่/เอฟเฟกต์ ให้ไม่หลุดเข้ากำแพง)
MAP.floor = {
  x0: T,
  y0: NH,
  x1: MAP.width - T,
  y1: MAP.height - T,
};
