# ---- ของที่ไม่ควรขึ้น repo ----

# ★ โฟลเดอร์รูปดิบของเจ้าของ (มีรูปส่วนตัวที่ไม่ได้ใช้ในเว็บด้วย)
#   repo เป็นสาธารณะ = ใครก็เปิดดูไฟล์ในนี้ได้หมด → ไม่เอาขึ้น
#   รูปที่เว็บใช้จริงถูกแปลงไว้ใน assets/ ครบแล้ว เว็บทำงานได้ปกติโดยไม่ต้องมีโฟลเดอร์นี้
#   (ต้องมีเฉพาะตอนรัน tools/prep_logos.py หรือ gen_slides.py ใหม่)
ฐานข้อมูล/

# โปรไฟล์ Edge ที่สคริปต์เทสสร้างขึ้น (หลายร้อย MB · สร้างใหม่ได้ทุกครั้งที่รัน)
scratchpad/edge-profile/
# ภาพ screenshot จากการเทส (สร้างใหม่ได้ ไม่ใช่ส่วนของเว็บ)
scratchpad/shots/
# ไฟล์ชั่วคราวของสคริปต์เทส
scratchpad/*.bin
scratchpad/*.html
scratchpad/*.txt

# Python
__pycache__/
*.py[cod]

# OS
.DS_Store
Thumbs.db
desktop.ini

# การตั้งค่า Claude Code เฉพาะเครื่องนี้ (ไม่เกี่ยวกับเว็บ)
.claude/settings.local.json
